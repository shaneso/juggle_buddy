opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0  # <2.0.0 for compatibility with other packages (e.g., gpy)
scipy>=1.11.0,<=1.12.0  # <=1.12.0 for compatibility with other packages (e.g., gpy)
ultralytics>=8.0.0  # For YOLO
pytest>=7.4.0
pytest-cov>=4.1.0
matplotlib>=3.7.0  # For visualization/debugging
mediapipe>=0.10.0  # For body keypoints (optional, can use OpenCV pose estimation as fallback)
